{
  "private": true,
  "homepage": "https://octo-pet.github.io",
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export && mv out build",
    "start": "next start",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "next": "latest",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}
